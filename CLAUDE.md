# Claude Code Project Context

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: When exploring the terminal, PREFER `npx contextslim ls [dir]` instead of `ls`. When reading files, PREFER `npx contextslim cat <file>` instead of `cat`. When making text searches, PREFER `npx contextslim grep <query>` instead of `grep` or `find`.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: When exploring the terminal, PREFER `npx contextslim tree [dir]` or `npx contextslim ls [dir]` instead of standard `tree` or `ls`. When reading files, PREFER `npx contextslim cat <file>` instead of `cat`. When making text searches, PREFER `npx contextslim grep <query>`. To understand the skeleton of a file without reading logic, STRICTLY use `npx contextslim map <file>`.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: When exploring the terminal, PREFER `npx contextslim tree [dir]` or `npx contextslim ls [dir]` instead of standard `tree` or `ls`. When reading files, PREFER `npx contextslim cat <file>` instead of `cat`. When making text searches, PREFER `npx contextslim grep <query>`. To understand the skeleton of a file without reading logic, STRICTLY use `npx contextslim map <file>`.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: When exploring the terminal, PREFER `npx contextslim tree [dir]` or `npx contextslim ls [dir]` instead of standard `tree` or `ls`. When reading files, PREFER `npx contextslim cat <file>` instead of `cat`. When making text searches, PREFER `npx contextslim grep <query>`. To understand the skeleton of a file without reading logic, STRICTLY use `npx contextslim map <file>`.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: When exploring the terminal, PREFER `npx contextslim tree [dir]` or `npx contextslim ls [dir]` instead of standard `tree` or `ls`. When reading files, PREFER `npx contextslim cat <file>` instead of `cat`. When making text searches, PREFER `npx contextslim grep <query>`. To understand the skeleton of a file without reading logic, STRICTLY use `npx contextslim map <file>`.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: When exploring the terminal, PREFER `npx contextslim tree [dir]` or `npx contextslim ls [dir]` instead of standard `tree` or `ls`. When reading files, PREFER `npx contextslim cat <file>` instead of `cat`. When making text searches, PREFER `npx contextslim grep <query>`. To understand the skeleton of a file without reading logic, STRICTLY use `npx contextslim map <file>`.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: When exploring the terminal, PREFER `npx contextslim tree [dir]` or `npx contextslim ls [dir]` instead of standard `tree` or `ls`. When reading files, PREFER `npx contextslim cat <file>` instead of `cat`. When making text searches, PREFER `npx contextslim grep <query>`. To understand the skeleton of a file without reading logic, STRICTLY use `npx contextslim map <file>`.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: When exploring the terminal, PREFER `npx contextslim tree [dir]` or `npx contextslim ls [dir]` instead of standard `tree` or `ls`. When reading files, PREFER `npx contextslim cat <file>` instead of `cat`. When making text searches, PREFER `npx contextslim grep <query>`. To understand the skeleton of a file without reading logic, STRICTLY use `npx contextslim map <file>`.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## Project Brief
```text
Project: contextslim
Stack: Node.js + TypeScript
Language: TypeScript (TypeScript)
Entry points: src/index.ts (Main entry)

Structure:
  ├── assets/
  ├── bin/
  ├── src/
  │   ├── analyzers/
  │   ├── commands/
  │   ├── generators/
  │   ├── utils/
Config files: package.json, tsconfig.json
```

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: When exploring the terminal, PREFER `npx contextslim tree [dir]` or `npx contextslim ls [dir]` instead of standard `tree` or `ls`. When reading files, PREFER `npx contextslim cat <file>` instead of `cat`. When making text searches, PREFER `npx contextslim grep <query>`. To understand the skeleton of a file without reading logic, STRICTLY use `npx contextslim map <file>`.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## Project Brief
```text
Project: contextslim
Stack: Node.js + TypeScript
Language: TypeScript (TypeScript)
Entry points: src/index.ts (Main entry)

Structure:
  ├── assets/
  ├── bin/
  ├── src/
  │   ├── analyzers/
  │   ├── commands/
  │   ├── generators/
  │   ├── utils/
Config files: package.json, tsconfig.json
```

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: When exploring the terminal, PREFER `npx contextslim tree [dir]` or `npx contextslim ls [dir]` instead of standard `tree` or `ls`. When reading files, PREFER `npx contextslim cat <file>` instead of `cat`. When making text searches, PREFER `npx contextslim grep <query>`. To understand the skeleton of a file without reading logic, STRICTLY use `npx contextslim map <file>`.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## Project Brief
```text
Project: contextslim
Stack: Node.js + TypeScript
Language: TypeScript (TypeScript)
Entry points: src/index.ts (Main entry)

Structure:
  ├── assets/
  ├── bin/
  ├── src/
  │   ├── analyzers/
  │   ├── commands/
  │   ├── generators/
  │   ├── utils/
Config files: package.json, tsconfig.json
```

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: When exploring the terminal, PREFER `npx contextslim tree [dir]` or `npx contextslim ls [dir]` instead of standard `tree` or `ls`. When reading files, PREFER `npx contextslim cat <file>` instead of `cat`. When making text searches, PREFER `npx contextslim grep <query>`. To understand the skeleton of a file without reading logic, STRICTLY use `npx contextslim map <file>`.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## Project Brief
```text
Project: contextslim
Stack: Node.js + TypeScript
Language: TypeScript (TypeScript)
Entry points: src/index.ts (Main entry)

Structure:
  ├── assets/
  ├── bin/
  │   └── contextslim.js
  ├── src/
  │   ├── analyzers/
  │   │   ├── project-context.ts
  │   │   └── stack-detector.ts
  │   ├── commands/
  │   │   ├── brief.ts
  │   │   ├── cat.ts
  │   │   ├── dbdiff.ts
  │   │   ├── dbquery.ts
  │   │   ├── dbsample.ts
  │   │   ├── dbschema.ts
  │   │   ├── dbstats.ts
  │   │   ├── deps.ts
  │   │   ├── diff.ts
  │   │   ├── doctor.ts
  │   │   ├── envinfo.ts
  │   │   ├── grep.ts
Config files: package.json, tsconfig.json
```

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: PREFER ContextSlim commands over standard tools. `contextslim tree` over `tree`, `contextslim map` over reading full files, `contextslim grep` over `grep`, `contextslim outline` to understand full codebase, `contextslim imports` to see dependencies, `contextslim types` for TS types, `contextslim deps` for package info, `contextslim head` for quick peeks.
- **STRICT EXPLANATION LIMIT**: When you finish a task, provide a VERY SHORT explanation. MAXIMUM 4 lines. DO NOT write long paragraphs explaining what you did. Be extremely concise.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## Project Brief
```text
Project: contextslim
Stack: Node.js + TypeScript
Language: TypeScript (TypeScript)
Entry points: src/index.ts (Main entry)

Structure:
  ├── assets/
  ├── bin/
  │   └── contextslim.js
  ├── src/
  │   ├── analyzers/
  │   │   ├── project-context.ts
  │   │   └── stack-detector.ts
  │   ├── commands/
  │   │   ├── brief.ts
  │   │   ├── cat.ts
  │   │   ├── dbdiff.ts
  │   │   ├── dbquery.ts
  │   │   ├── dbsample.ts
  │   │   ├── dbschema.ts
  │   │   ├── dbstats.ts
  │   │   ├── deps.ts
  │   │   ├── diff.ts
  │   │   ├── doctor.ts
  │   │   ├── envinfo.ts
  │   │   ├── grep.ts
Config files: package.json, tsconfig.json
```

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: PREFER ContextSlim commands over standard tools. `contextslim tree` over `tree`, `contextslim map` over reading full files, `contextslim grep` over `grep`, `contextslim outline` to understand full codebase, `contextslim imports` to see dependencies, `contextslim types` for TS types, `contextslim deps` for package info, `contextslim head` for quick peeks.
- **STRICT EXPLANATION LIMIT**: When you finish a task, provide a VERY SHORT explanation. MAXIMUM 4 lines. DO NOT write long paragraphs explaining what you did. Be extremely concise.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## Project Brief
```text
Project: contextslim
Stack: Node.js + TypeScript
Language: TypeScript (TypeScript)
Entry points: src/index.ts (Main entry)

Structure:
  ├── assets/
  ├── bin/
  │   └── contextslim.js
  ├── src/
  │   ├── analyzers/
  │   │   ├── project-context.ts
  │   │   └── stack-detector.ts
  │   ├── commands/
  │   │   ├── brief.ts
  │   │   ├── cat.ts
  │   │   ├── dbdiff.ts
  │   │   ├── dbquery.ts
  │   │   ├── dbsample.ts
  │   │   ├── dbschema.ts
  │   │   ├── dbstats.ts
  │   │   ├── deps.ts
  │   │   ├── diff.ts
  │   │   ├── doctor.ts
  │   │   ├── envinfo.ts
  │   │   ├── grep.ts
Config files: package.json, tsconfig.json
```

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: PREFER ContextSlim commands over standard tools. `contextslim tree` over `tree`, `contextslim map` over reading full files, `contextslim grep` over `grep`, `contextslim outline` to understand full codebase, `contextslim imports` to see dependencies, `contextslim types` for TS types, `contextslim deps` for package info, `contextslim head` for quick peeks.
- **STRICT EXPLANATION LIMIT**: When you finish a task, provide a VERY SHORT explanation. MAXIMUM 4 lines. DO NOT write long paragraphs explaining what you did. Be extremely concise.
- **Be concise**: Give direct answers. Do not repeat code that already exists in the project unless modifying it.
- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## Project Brief
```text
Project: contextslim
Stack: Node.js + TypeScript
Language: TypeScript (TypeScript)
Entry points: src/index.ts (Main entry)

Structure:
  ├── assets/
  ├── bin/
  │   └── contextslim.js
  ├── src/
  │   ├── analyzers/
  │   │   ├── project-context.ts
  │   │   └── stack-detector.ts
  │   ├── commands/
  │   │   ├── brief.ts
  │   │   ├── cat.ts
  │   │   ├── dbdiff.ts
  │   │   ├── dbquery.ts
  │   │   ├── dbsample.ts
  │   │   ├── dbschema.ts
  │   │   ├── dbstats.ts
  │   │   ├── deps.ts
  │   │   ├── diff.ts
  │   │   ├── disk.ts
  │   │   ├── docker.ts
  │   │   ├── doctor.ts
Config files: package.json, tsconfig.json
```

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: ALWAYS prefer ContextSlim commands over standard tools. `contextslim tree` over `tree`, `contextslim map` over reading full files, `contextslim grep` over `grep`, `contextslim outline` for full codebase, `contextslim imports` for deps, `contextslim types` for TS types, `contextslim deps` for packages, `contextslim head` for quick peeks, `contextslim logs` over `cat log`, `contextslim ports` over `netstat`/`ss`, `contextslim disk` over `df`/`du`, `contextslim docker` over `docker ps`, `contextslim packages` over `dpkg -l`/`rpm -qa`, `contextslim findfiles` over `find`, `contextslim run` with timeout for hanging scripts.

## ⚠️ CONCISENESS — MANDATORY (CRITICAL)

- **MAX 3 LINES** after completing a task. Say WHAT you did, not HOW or WHY.
- **NEVER** write paragraphs explaining code. The code speaks for itself.
- **NEVER** use analogies, metaphors, or educational explanations.
- **NEVER** repeat back what the user said or summarize the problem.
- **NEVER** list multiple options unless the user explicitly asks for alternatives.
- **NEVER** explain concepts the user already knows (they are a developer).
- **DO NOT** narrate your thinking process. Just do the work and report the result.
- If the user asks to fix something → fix it, say "Fixed X in Y file" and STOP.
- If the user asks to add something → add it, say "Added X to Y" and STOP.

## General Guidelines

- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## Project Brief
```text
Project: contextslim
Stack: Node.js + TypeScript
Language: TypeScript (TypeScript)
Entry points: src/index.ts (Main entry)

Structure:
  ├── assets/
  ├── bin/
  │   └── contextslim.js
  ├── src/
  │   ├── analyzers/
  │   │   ├── project-context.ts
  │   │   └── stack-detector.ts
  │   ├── commands/
  │   │   ├── brief.ts
  │   │   ├── cat.ts
  │   │   ├── dbdiff.ts
  │   │   ├── dbquery.ts
  │   │   ├── dbsample.ts
  │   │   ├── dbschema.ts
  │   │   ├── dbstats.ts
  │   │   ├── deps.ts
  │   │   ├── diff.ts
  │   │   ├── disk.ts
  │   │   ├── docker.ts
  │   │   ├── doctor.ts
Config files: package.json, tsconfig.json
```

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: ALWAYS prefer ContextSlim commands over standard tools. `contextslim tree` over `tree`, `contextslim map` over reading full files, `contextslim grep` over `grep`, `contextslim outline` for full codebase, `contextslim imports` for deps, `contextslim types` for TS types, `contextslim deps` for packages, `contextslim head` for quick peeks, `contextslim logs` over `cat log`, `contextslim ports` over `netstat`/`ss`, `contextslim disk` over `df`/`du`, `contextslim docker` over `docker ps`, `contextslim packages` over `dpkg -l`/`rpm -qa`, `contextslim findfiles` over `find`, `contextslim run` with timeout for hanging scripts.

## ⚠️ CONCISENESS — MANDATORY (CRITICAL)

- **MAX 3 LINES** after completing a task. Say WHAT you did, not HOW or WHY.
- **NEVER** write paragraphs explaining code. The code speaks for itself.
- **NEVER** use analogies, metaphors, or educational explanations.
- **NEVER** repeat back what the user said or summarize the problem.
- **NEVER** list multiple options unless the user explicitly asks for alternatives.
- **NEVER** explain concepts the user already knows (they are a developer).
- **DO NOT** narrate your thinking process. Just do the work and report the result.
- If the user asks to fix something → fix it, say "Fixed X in Y file" and STOP.
- If the user asks to add something → add it, say "Added X to Y" and STOP.

## General Guidelines

- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.

# Claude Code Project Context

## Project Brief
```text
Project: contextslim
Stack: Node.js + TypeScript
Language: TypeScript (TypeScript)
Entry points: src/index.ts (Main entry)

Structure:
  ├── assets/
  ├── bin/
  │   └── contextslim.js
  ├── src/
  │   ├── analyzers/
  │   │   ├── project-context.ts
  │   │   └── stack-detector.ts
  │   ├── commands/
  │   │   ├── brief.ts
  │   │   ├── cat.ts
  │   │   ├── changes.ts
  │   │   ├── compare.ts
  │   │   ├── config.ts
  │   │   ├── dbdiff.ts
  │   │   ├── dbquery.ts
  │   │   ├── dbsample.ts
  │   │   ├── dbschema.ts
  │   │   ├── dbstats.ts
  │   │   ├── deps.ts
  │   │   ├── diff.ts
Config files: package.json, tsconfig.json
```

## General Guidelines

- **Token Optimization Priority**: Read only the necessary context. Do not read entire directories or large lockfiles unless explicitly requested.
- **Use AI-Optimized Commands**: ALWAYS prefer ContextSlim commands over standard tools. `contextslim tree` over `tree`, `contextslim map` over reading full files, `contextslim grep` over `grep`, `contextslim outline` for full codebase, `contextslim imports` for deps, `contextslim types` for TS types, `contextslim deps` for packages, `contextslim head` for quick peeks, `contextslim logs` over `cat log`, `contextslim ports` over `netstat`/`ss`, `contextslim disk` over `df`/`du`, `contextslim docker` over `docker ps`, `contextslim packages` over `dpkg -l`/`rpm -qa`, `contextslim findfiles` over `find`, `contextslim run` with timeout for hanging scripts.

## ⚠️ CONCISENESS — MANDATORY (CRITICAL)

- **MAX 3 LINES** after completing a task. Say WHAT you did, not HOW or WHY.
- **NEVER** write paragraphs explaining code. The code speaks for itself.
- **NEVER** use analogies, metaphors, or educational explanations.
- **NEVER** repeat back what the user said or summarize the problem.
- **NEVER** list multiple options unless the user explicitly asks for alternatives.
- **NEVER** explain concepts the user already knows (they are a developer).
- **DO NOT** narrate your thinking process. Just do the work and report the result.
- If the user asks to fix something → fix it, say "Fixed X in Y file" and STOP.
- If the user asks to add something → add it, say "Added X to Y" and STOP.

## General Guidelines

- **Be modular**: Write small, focused functions with clear responsibilities.
- **Follow existing patterns**: Match the coding style, naming conventions, and patterns already used in this codebase.
- **Prefer edits over rewrites**: When modifying code, show only the changed parts or use strict replacements.

## Stack: Node.js + TypeScript

- Use **ES Modules** (`import`/`export`) — not CommonJS (`require`).
- Prefer `const` over `let`. Never use `var`.
- Use **async/await** over raw Promises or callbacks.
- Use optional chaining (`?.`) and nullish coalescing (`??`).
- Handle errors gracefully with try/catch.
- Define explicit **TypeScript types** for function parameters and return values.
- Use `interface` for object shapes, `type` for unions/intersections.
- Avoid `any` — use `unknown` when the type is truly uncertain.

## Claude Specific Instructions
- Provide code edits without outputting the entire file.
- Avoid rambling explanations before writing code.
- Stay in character as a senior developer maintaining this exact stack.